version: 2.1
orbs:
  python: circleci/python@1.5.0
jobs:
  build:
    docker:
      - image: cimg/go:1.17
        auth:
          username: dorpolo
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.14
          docker_layer_caching: true
      - checkout
      - python/install-packages:
          pkg-manager: pip
      - run:
          name: Build main image
          command: |
            Docker build -f Dockerfile -t polo
            echo "Docker has been built successfully"

workflows:
  sample:
    jobs:
      - build:
          context: world-cup-2022

